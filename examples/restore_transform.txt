{
    'Headers': self.Headers != null ? self.Headers.map(h, {'Key': h.Key, 'Value': base64.decode(h.Value)}) : [],
    'Key': self.Key != null ? base64.decode(self.Key) : b'',
    'Value': base64.decode(self.Value),
    'Timestamp': now(),
    'TimestampType': 1
}
