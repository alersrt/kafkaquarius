{
    'Headers': self.Headers != null ? self.Headers.map(h, {'Key': h.Key, 'Value': base64.decode(h.Value)}) : [],
    'Key': self.Key != null ? bytes(self.Key) : b'',
    'Value': marshal(self.Value),
    'Timestamp': now(),
    'TimestampType': 1
}
