{
    'Headers': [{"Key": 'Test', "Value": bytes(uuid())}],
    'Key': bytes(uuid()),
    'Value': marshal({
        'SentTs': now(),
        'Version': self.version,
        'Kinds': self.value.map(s, {'String': s})
    })
}
